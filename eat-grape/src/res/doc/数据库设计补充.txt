数据字典共新增2个表（这2个表的字段和t_shop_type表类似）：

1、新增送餐备注短语表（用作下订单时快捷选择一些备注语）
2、新增菜品特性表（表示菜单表里的feature）


其他补充：

3、新增前台导航栏表
4、新增前台友情链接表
5、订单表t_order_lib
      （1）trade_state字段加一个标示码，2：已送出/正在配送
      （2）加一个字段，notify_type:通知类型（0：短信通知，1：不需通知，顾客自己在电脑上查看）
      （3）加一个字段，remark：订单备注，相当于给商家的一些要求留言，如1点前必须送到、不要花椒、清淡一点等[varchar(200)]
      （4）加一个字段，送餐时间：send_time
      （5）是否考虑对游客允许订餐，即未登录也可进行下单，如果允许，则应加上以下几个字段：
               ①、地址（not null、varchar200）
               ②、电话（not null、varchar15）
               ③、备选电话（null、varchar15）
               ④、送餐时间（null、varchar20）
6、新增收藏表（上一次已补充过）
7、系统公告表t_system_notice新增字段target标示通知目标：0：商家，1：前台。（上一次已补充过）
8、商家的每一个商店的商店表加一字段：起送价（up_send_price）
9、因为考虑到一些商家的不同商店对不同学校或不同小区送餐价格和起送价的不同，所以要对商店表和t_community表进行关联，我之前做了个关联，不知道是否恰当。
10、城市关联表重新设计，缩为1个表，用父子管理进行管理，其中需要改造的表包括（t_province、t_city、t_area、t_distinct），统一归纳为一个表：t_distinct，顶级区域的父级区域[parent_id]用0标示）